# APTLEARN Interview Prep: Java + Spring Boot
## 75 Live Coding Questions for Full-Stack Development

---

## 📋 Overview

**Total Questions:** 75  
**Technology:** Java + Spring Boot  
**Interview Format:** Live coding, screen share  
**Time Range:** 5-45 minutes per question  
**Difficulty Levels:** 4 tiers (Junior → Lead)

---

## 🎯 Tier 1: Junior Level (20 questions)
**Target:** 0-2 years experience | Time: 5-15 min each

### 1. Hello World Endpoint ⭐
**Time:** 5-15 min  
**Tests:** Basic routing, GET endpoint

**Challenge:**
```java
// Create a Spring Boot controller for: Hello World Endpoint
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 2. GET Endpoint with Params ⭐
**Time:** 5-15 min  
**Tests:** Route parameters, query params

**Challenge:**
```java
// Create a Spring Boot controller for: GET Endpoint with Params
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 3. POST Endpoint with Body ⭐
**Time:** 5-15 min  
**Tests:** Request body, POST handling

**Challenge:**
```java
// Create a Spring Boot controller for: POST Endpoint with Body
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 4. PUT Endpoint ⭐
**Time:** 5-15 min  
**Tests:** Update operations, PUT method

**Challenge:**
```java
// Create a Spring Boot controller for: PUT Endpoint
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 5. DELETE Endpoint ⭐
**Time:** 5-15 min  
**Tests:** Delete operations, DELETE method

**Challenge:**
```java
// Create a Spring Boot controller for: DELETE Endpoint
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 6. Middleware Basics ⭐
**Time:** 5-15 min  
**Tests:** Middleware, request pipeline

**Challenge:**
```java
// Create a Spring Boot controller for: Middleware Basics
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 7. Error Handling Middleware ⭐
**Time:** 5-15 min  
**Tests:** Error handling, error responses

**Challenge:**
```java
// Create a Spring Boot controller for: Error Handling Middleware
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 8. Environment Variables ⭐
**Time:** 5-15 min  
**Tests:** Config management, env vars

**Challenge:**
```java
// Create a Spring Boot controller for: Environment Variables
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 9. CORS Setup ⭐
**Time:** 5-15 min  
**Tests:** Cross-origin requests, CORS

**Challenge:**
```java
// Create a Spring Boot controller for: CORS Setup
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 10. Request Validation ⭐
**Time:** 5-15 min  
**Tests:** Input validation, validation middleware

**Challenge:**
```java
// Create a Spring Boot controller for: Request Validation
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 11. Response Formatting ⭐
**Time:** 5-15 min  
**Tests:** Response structure, JSON responses

**Challenge:**
```java
// Create a Spring Boot controller for: Response Formatting
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 12. Query Parameters ⭐
**Time:** 5-15 min  
**Tests:** Query string parsing

**Challenge:**
```java
// Create a Spring Boot controller for: Query Parameters
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 13. Path Parameters ⭐
**Time:** 5-15 min  
**Tests:** Route parameters, dynamic routes

**Challenge:**
```java
// Create a Spring Boot controller for: Path Parameters
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 14. Request Body Parsing ⭐
**Time:** 5-15 min  
**Tests:** Body parsing, JSON parsing

**Challenge:**
```java
// Create a Spring Boot controller for: Request Body Parsing
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 15. Static File Serving ⭐
**Time:** 5-15 min  
**Tests:** File serving, static assets

**Challenge:**
```java
// Create a Spring Boot controller for: Static File Serving
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 16. Simple Logging ⭐
**Time:** 5-15 min  
**Tests:** Logging, console logging

**Challenge:**
```java
// Create a Spring Boot controller for: Simple Logging
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 17. Health Check Endpoint ⭐
**Time:** 5-15 min  
**Tests:** Health checks, monitoring

**Challenge:**
```java
// Create a Spring Boot controller for: Health Check Endpoint
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 18. Basic Authentication ⭐
**Time:** 5-15 min  
**Tests:** Auth basics, username/password

**Challenge:**
```java
// Create a Spring Boot controller for: Basic Authentication
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 19. Password Hashing ⭐
**Time:** 5-15 min  
**Tests:** Password security, hashing

**Challenge:**
```java
// Create a Spring Boot controller for: Password Hashing
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 20. JWT Token Creation ⭐
**Time:** 5-15 min  
**Tests:** JWT, token generation

**Challenge:**
```java
// Create a Spring Boot controller for: JWT Token Creation
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---

---

## 🎯 Tier 2: Mid-Level (25 questions)  
**Target:** 2-4 years experience | Time: 15-25 min each

### 21. Database Connection ⭐⭐
**Time:** 15-25 min  
**Tests:** DB connection, connection pooling

**Challenge:**
```java
// Create a Spring Boot controller for: Database Connection
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 22. CRUD Operations ⭐⭐
**Time:** 15-25 min  
**Tests:** Create, read, update, delete

**Challenge:**
```java
// Create a Spring Boot controller for: CRUD Operations
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 23. One-to-Many Relationships ⭐⭐
**Time:** 15-25 min  
**Tests:** Database relationships

**Challenge:**
```java
// Create a Spring Boot controller for: One-to-Many Relationships
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 24. Many-to-Many Relationships ⭐⭐
**Time:** 15-25 min  
**Tests:** Junction tables, relationships

**Challenge:**
```java
// Create a Spring Boot controller for: Many-to-Many Relationships
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 25. Database Transactions ⭐⭐
**Time:** 15-25 min  
**Tests:** Transactions, ACID

**Challenge:**
```java
// Create a Spring Boot controller for: Database Transactions
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 26. Query Optimization ⭐⭐
**Time:** 15-25 min  
**Tests:** SQL optimization, indexes

**Challenge:**
```java
// Create a Spring Boot controller for: Query Optimization
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 27. Pagination ⭐⭐
**Time:** 15-25 min  
**Tests:** Pagination, limit/offset

**Challenge:**
```java
// Create a Spring Boot controller for: Pagination
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 28. Filtering and Sorting ⭐⭐
**Time:** 15-25 min  
**Tests:** Data filtering, sorting

**Challenge:**
```java
// Create a Spring Boot controller for: Filtering and Sorting
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 29. Search Functionality ⭐⭐
**Time:** 15-25 min  
**Tests:** Search, full-text search

**Challenge:**
```java
// Create a Spring Boot controller for: Search Functionality
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 30. File Upload Handling ⭐⭐
**Time:** 15-25 min  
**Tests:** File uploads, multipart/form-data

**Challenge:**
```java
// Create a Spring Boot controller for: File Upload Handling
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 31. JWT Authentication ⭐⭐
**Time:** 15-25 min  
**Tests:** JWT, token validation

**Challenge:**
```java
// Create a Spring Boot controller for: JWT Authentication
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 32. Refresh Tokens ⭐⭐
**Time:** 15-25 min  
**Tests:** Token refresh, security

**Challenge:**
```java
// Create a Spring Boot controller for: Refresh Tokens
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 33. Role-Based Access Control ⭐⭐
**Time:** 15-25 min  
**Tests:** RBAC, permissions

**Challenge:**
```java
// Create a Spring Boot controller for: Role-Based Access Control
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 34. API Versioning ⭐⭐
**Time:** 15-25 min  
**Tests:** Versioning, backward compatibility

**Challenge:**
```java
// Create a Spring Boot controller for: API Versioning
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 35. Rate Limiting ⭐⭐
**Time:** 15-25 min  
**Tests:** Rate limiting, throttling

**Challenge:**
```java
// Create a Spring Boot controller for: Rate Limiting
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 36. Caching (Redis) ⭐⭐
**Time:** 15-25 min  
**Tests:** Caching, Redis integration

**Challenge:**
```java
// Create a Spring Boot controller for: Caching (Redis)
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 37. Session Management ⭐⭐
**Time:** 15-25 min  
**Tests:** Sessions, session storage

**Challenge:**
```java
// Create a Spring Boot controller for: Session Management
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 38. Cookie Handling ⭐⭐
**Time:** 15-25 min  
**Tests:** Cookies, secure cookies

**Challenge:**
```java
// Create a Spring Boot controller for: Cookie Handling
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 39. Email Sending ⭐⭐
**Time:** 15-25 min  
**Tests:** Email, SMTP

**Challenge:**
```java
// Create a Spring Boot controller for: Email Sending
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 40. Background Jobs ⭐⭐
**Time:** 15-25 min  
**Tests:** Job queues, async processing

**Challenge:**
```java
// Create a Spring Boot controller for: Background Jobs
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 41. Scheduled Tasks ⭐⭐
**Time:** 15-25 min  
**Tests:** Cron jobs, scheduled tasks

**Challenge:**
```java
// Create a Spring Boot controller for: Scheduled Tasks
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 42. WebSocket Server ⭐⭐
**Time:** 15-25 min  
**Tests:** WebSockets, real-time

**Challenge:**
```java
// Create a Spring Boot controller for: WebSocket Server
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 43. Real-Time Chat ⭐⭐
**Time:** 15-25 min  
**Tests:** Real-time communication

**Challenge:**
```java
// Create a Spring Boot controller for: Real-Time Chat
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 44. API Documentation ⭐⭐
**Time:** 15-25 min  
**Tests:** OpenAPI, Swagger

**Challenge:**
```java
// Create a Spring Boot controller for: API Documentation
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 45. Input Sanitization ⭐⭐
**Time:** 15-25 min  
**Tests:** Security, XSS prevention

**Challenge:**
```java
// Create a Spring Boot controller for: Input Sanitization
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---

---

## 🎯 Tier 3: Senior Level (20 questions)
**Target:** 4-6 years experience | Time: 25-35 min each

### 46. OAuth2 Implementation ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** OAuth2, authentication flows

**Challenge:**
```java
// Create a Spring Boot controller for: OAuth2 Implementation
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 47. Microservices Communication ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Service communication, APIs

**Challenge:**
```java
// Create a Spring Boot controller for: Microservices Communication
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 48. Message Queue (RabbitMQ/Kafka) ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Message queues, async messaging

**Challenge:**
```java
// Create a Spring Boot controller for: Message Queue (RabbitMQ/Kafka)
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 49. Event-Driven Architecture ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Events, event sourcing

**Challenge:**
```java
// Create a Spring Boot controller for: Event-Driven Architecture
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 50. CQRS Pattern ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Command Query Responsibility Segregation

**Challenge:**
```java
// Create a Spring Boot controller for: CQRS Pattern
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 51. Repository Pattern ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Data access, abstraction

**Challenge:**
```java
// Create a Spring Boot controller for: Repository Pattern
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 52. Unit Testing ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Unit tests, test coverage

**Challenge:**
```java
// Create a Spring Boot controller for: Unit Testing
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 53. Integration Testing ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Integration tests, test databases

**Challenge:**
```java
// Create a Spring Boot controller for: Integration Testing
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 54. Test Coverage ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Code coverage, testing metrics

**Challenge:**
```java
// Create a Spring Boot controller for: Test Coverage
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 55. Database Migrations ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Migrations, schema changes

**Challenge:**
```java
// Create a Spring Boot controller for: Database Migrations
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 56. Database Seeding ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Seed data, test data

**Challenge:**
```java
// Create a Spring Boot controller for: Database Seeding
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 57. Multi-Tenancy ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Multi-tenant architecture

**Challenge:**
```java
// Create a Spring Boot controller for: Multi-Tenancy
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 58. Soft Deletes ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Soft deletes, data retention

**Challenge:**
```java
// Create a Spring Boot controller for: Soft Deletes
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 59. Audit Logging ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Audit trails, logging

**Challenge:**
```java
// Create a Spring Boot controller for: Audit Logging
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 60. Request Throttling ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Throttling, rate limiting

**Challenge:**
```java
// Create a Spring Boot controller for: Request Throttling
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 61. API Gateway ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Gateway pattern, routing

**Challenge:**
```java
// Create a Spring Boot controller for: API Gateway
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 62. Service Discovery ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Service registry, discovery

**Challenge:**
```java
// Create a Spring Boot controller for: Service Discovery
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 63. Circuit Breaker Pattern ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Resilience, fault tolerance

**Challenge:**
```java
// Create a Spring Boot controller for: Circuit Breaker Pattern
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 64. Distributed Tracing ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** Tracing, observability

**Challenge:**
```java
// Create a Spring Boot controller for: Distributed Tracing
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 65. Performance Monitoring ⭐⭐⭐
**Time:** 25-35 min  
**Tests:** APM, monitoring

**Challenge:**
```java
// Create a Spring Boot controller for: Performance Monitoring
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---

---

## 🎯 Tier 4: Lead Level (10 questions)
**Target:** 6+ years experience | Time: 35-45 min each

### 66. System Design (URL Shortener) ⭐⭐⭐⭐
**Time:** 35-45 min  
**Tests:** System design, scalability

**Challenge:**
```java
// Create a Spring Boot controller for: System Design (URL Shortener)
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 67. System Design (Chat App) ⭐⭐⭐⭐
**Time:** 35-45 min  
**Tests:** Real-time systems, WebSockets

**Challenge:**
```java
// Create a Spring Boot controller for: System Design (Chat App)
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 68. Database Sharding ⭐⭐⭐⭐
**Time:** 35-45 min  
**Tests:** Sharding, horizontal scaling

**Challenge:**
```java
// Create a Spring Boot controller for: Database Sharding
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 69. Read Replicas ⭐⭐⭐⭐
**Time:** 35-45 min  
**Tests:** Replication, read scaling

**Challenge:**
```java
// Create a Spring Boot controller for: Read Replicas
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 70. Load Balancing ⭐⭐⭐⭐
**Time:** 35-45 min  
**Tests:** Load balancers, distribution

**Challenge:**
```java
// Create a Spring Boot controller for: Load Balancing
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 71. Horizontal Scaling ⭐⭐⭐⭐
**Time:** 35-45 min  
**Tests:** Scaling, infrastructure

**Challenge:**
```java
// Create a Spring Boot controller for: Horizontal Scaling
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 72. Deployment Strategies ⭐⭐⭐⭐
**Time:** 35-45 min  
**Tests:** Blue-green, canary deployments

**Challenge:**
```java
// Create a Spring Boot controller for: Deployment Strategies
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 73. Container Orchestration ⭐⭐⭐⭐
**Time:** 35-45 min  
**Tests:** Kubernetes, Docker

**Challenge:**
```java
// Create a Spring Boot controller for: Container Orchestration
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 74. CI/CD Pipeline ⭐⭐⭐⭐
**Time:** 35-45 min  
**Tests:** Continuous integration, deployment

**Challenge:**
```java
// Create a Spring Boot controller for: CI/CD Pipeline
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---
### 75. Security Best Practices ⭐⭐⭐⭐
**Time:** 35-45 min  
**Tests:** Security, OWASP, vulnerabilities

**Challenge:**
```java
// Create a Spring Boot controller for: Security Best Practices
// Use annotations and dependency injection
// Include proper error handling
```

**What interviewers look for:**
```java
@RestController
@RequestMapping("/api")
public class MyController {
    
    @GetMapping("/hello")
    public ResponseEntity<String> hello() {
        return ResponseEntity.ok("Hello World");
    }
}
```

---

---

## 📊 Question Distribution

**By Category:**
- Fundamentals: 15 questions
- Intermediate Concepts: 20 questions  
- Advanced Patterns: 20 questions
- Architecture & Performance: 12 questions
- Real-world Scenarios: 8 questions

**By Type:**
- Component/Function Building: 30%
- State Management: 15%
- API/Backend Integration: 20%
- Testing & Quality: 10%
- Performance & Optimization: 10%
- Architecture & Design: 15%
