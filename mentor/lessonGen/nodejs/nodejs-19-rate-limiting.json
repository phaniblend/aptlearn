{
  "id": "nodejs-19-rate-limiting",
  "title": "Rate Limiting",
  "technology": "Nodejs",
  "difficulty": "mid",
  "language": "javascript",
  "status": "draft",
  "metadata": {
    "time_estimate": "20 minutes",
    "tests": "Rate limiting, middleware",
    "challenge_number": "19"
  },
  "flow": [
    {
      "stepId": "title",
      "mentorSays": "At the end of this lesson, you will be able to:\n\n1. Understand Rate Limiting in Nodejs\n2. Implement the solution using Rate limiting, middleware\n3. Apply Angular best practices and patterns\n4. Handle edge cases and error scenarios\n5. Write clean, maintainable Nodejs code",
      "action": "continue",
      "next": "problem-illustration"
    },
    {
      "stepId": "problem-illustration",
      "mentorSays": "Let's understand what this Rate Limiting challenge asks for.\n\n**Challenge Description:**\n// Implement basic rate limiting:\n// - Max 5 requests per minute per IP\n// - Return 429 Too Many Requests when exceeded\n// - Reset after 1 minute\n// - Track by IP address\n\n**What we need to build:**\nThis challenge tests your understanding of Rate limiting, middleware.\n\n**Time estimate:** 20 minutes\n**Difficulty level:** Mid\n\n**Expected Solution Overview:**\n```typescript\nconst rateLimit = {};\n\nconst rateLimiter = (req, res, next) => {\n  const ip = req.ip;\n  const now = Date.now();\n  const windowMs = 60 * 1000; // 1 minute\n  const maxRequests = 5;\n\n  if (!rateLimit[ip]) {\n    rateLimit[ip] = [];\n  }\n\n  // Remove old requests outside window\n  rateLimit[ip] = rateLimit...\n```\n\nThis is a practical interview question that tests your understanding of core Angular concepts and your ability to implement them correctly.",
      "example": "// Implement basic rate limiting:\n// - Max 5 requests per minute per IP\n// - Return 429 Too Many Requests when exceeded\n// - Reset after 1 minute\n// - Track by IP address",
      "action": "continue",
      "next": "thinking-challenge"
    },
    {
      "stepId": "thinking-challenge",
      "mentorSays": "Now that you understand what the problem wants, here's the real question:\n\nHow would YOU solve this Rate Limiting challenge?\n\nThink about it for a moment. What approach feels natural to you?",
      "choices": [
        {
          "label": "I'll start with the basic approach and build up",
          "next": "explore-approach-1"
        },
        {
          "label": "I want to see the optimal solution directly",
          "next": "explore-optimal"
        },
        {
          "label": "I need more context about the problem",
          "next": "problem-illustration"
        }
      ]
    },
    {
      "stepId": "explore-approach-1",
      "mentorSays": "Good! Starting with a basic approach is a solid strategy. Let's build it step by step, then we'll see the complete solution.",
      "action": "continue",
      "next": "explore-optimal"
    },
    {
      "stepId": "explore-optimal",
      "mentorSays": "Perfect! Here's the optimal solution approach:\n\n```typescript\nconst rateLimit = {};\n\nconst rateLimiter = (req, res, next) => {\n  const ip = req.ip;\n  const now = Date.now();\n  const windowMs = 60 * 1000; // 1 minute\n  const maxRequests = 5;\n\n  if (!rateLimit[ip]) {\n    rateLimit[ip] = [];\n  }\n\n  // Remove old requests outside window\n  rateLimit[ip] = rateLimit[ip].filter(time => now - time < windowMs);\n\n  if (rateLimit[ip].length >= maxRequests) {\n    return res.status(429).json({ error: 'Too many requests' });\n  }\n\n  rateLimit[ip].push(now);\n  next();\n};\n\napp.use(rateLimiter);\n```\n\nThis solution demonstrates best practices for Nodejs development and shows how to properly implement Rate limiting, middleware.",
      "example": "const rateLimit = {};\n\nconst rateLimiter = (req, res, next) => {\n  const ip = req.ip;\n  const now = Date.now();\n  const windowMs = 60 * 1000; // 1 minute\n  const maxRequests = 5;\n\n  if (!rateLimit[ip]) {\n    rateLimit[ip] = [];\n  }\n\n  // Remove old requests outside window\n  rateLimit[ip] = rateLimit[ip].filter(time => now - time < windowMs);\n\n  if (rateLimit[ip].length >= maxRequests) {\n    return res.status(429).json({ error: 'Too many requests' });\n  }\n\n  rateLimit[ip].push(now);\n  next();\n};\n\napp.use(rateLimiter);",
      "action": "continue",
      "next": "language-selection"
    },
    {
      "stepId": "language-selection",
      "mentorSays": "Great! Now let's code this solution. Which programming language would you like to use?",
      "choices": [
        {
          "label": "JavaScript",
          "next": "variable-check-js"
        },
        {
          "label": "TypeScript",
          "next": "variable-check-ts"
        },
        {
          "label": "Python",
          "next": "variable-check-python"
        }
      ]
    },
    {
      "stepId": "coding-start-ts",
      "mentorSays": "Let's implement the solution:",
      "example": "const rateLimit = {};\n\nconst rateLimiter = (req, res, next) => {\n  const ip = req.ip;\n  const now = Date.now();\n  const windowMs = 60 * 1000; // 1 minute\n  const maxRequests = 5;\n\n  if (!rateLimit[ip]) {\n    rateLimit[ip] = [];\n  }\n\n  // Remove old requests outside window\n  rateLimit[ip] = rateLimit[ip].filter(time => now - time < windowMs);\n\n  if (rateLimit[ip].length >= maxRequests) {\n    return res.status(429).json({ error: 'Too many requests' });\n  }\n\n  rateLimit[ip].push(now);\n  next();\n};\n\napp.use(rateLimiter);",
      "action": "continue",
      "next": "test-code-ts"
    },
    {
      "stepId": "test-code-ts",
      "mentorSays": "Perfect! Now test your code. Make sure it works correctly for the Rate Limiting challenge.\n\n**Test Cases:**\n1. Test with basic input\n2. Test with edge cases\n3. Test with empty/null values\n4. Verify the output matches expected behavior\n\nTry running it and verify the solution works as expected.",
      "example": "// Test cases for Rate Limiting\n// 1. Basic functionality\n// 2. Edge cases\n// 3. Error handling",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "final",
      "mentorSays": "🎉 Well done! You've completed the Rate Limiting challenge.\n\n**Key Takeaways:**\n- You've mastered Rate limiting, middleware\n- You understand how to implement Rate Limiting in Nodejs\n- You've applied best practices for Nodejs development\n- You can handle this type of interview question confidently\n\n**Time Complexity:** Varies based on implementation\n**Space Complexity:** Varies based on implementation\n\n**Related Challenges:**\n- Practice similar Nodejs concepts\n- Try variations of this challenge\n- Explore more advanced Nodejs patterns\n\nKeep practicing!",
      "action": "continue"
    }
  ]
}